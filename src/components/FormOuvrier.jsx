import React,{useState} from "react";
export const FormOuvrier=({ouvrier,onSubmit,onCancel,mode="add"})=>{const[data,setData]=useState(ouvrier?{...ouvrier}:{nom:"",type:"CDI",metier:"",statut:"Actif",ordre:"",separateurApres:false});const save=()=>{if(!data.nom||!data.metier)return alert("Nom et métier obligatoires");onSubmit(data)};return <div style={{display:"grid",gap:12}}><input placeholder="Nom" value={data.nom} onChange={e=>setData({...data,nom:e.target.value})}/><input placeholder="Métier" value={data.metier} onChange={e=>setData({...data,metier:e.target.value})}/><select value={data.type} onChange={e=>setData({...data,type:e.target.value})}><option>CDI</option><option>ST</option></select><label><input type="checkbox" checked={!!data.separateurApres} onChange={e=>setData({...data,separateurApres:e.target.checked})}/> Trait bleu après cette personne</label>{mode==="edit"&&<select value={data.statut} onChange={e=>setData({...data,statut:e.target.value})}><option>Actif</option><option>Archivé</option></select>}<div style={{display:"flex",gap:8}}><button onClick={onCancel}>Annuler</button><button onClick={save} style={{background:"#1e3a8a",color:"white"}}>{mode==="edit"?"Enregistrer":"Ajouter"}</button></div></div>};
